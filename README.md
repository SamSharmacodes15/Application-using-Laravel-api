# Laravel Simple CRUD API (Users)

This is a simple Laravel REST API project that performs basic CRUD operations using MySQL.
It includes two main API endpoints:

* **GET /api/users** → Fetch all users
* **POST /api/users** → Add a new user

## 🚀 Features

* Laravel 11 backend
* MySQL database
* API endpoints tested using Postman
* Simple CRUD structure

## 🛠️ Technologies Used

* Laravel (PHP Framework)
* PHP
* MySQL
* Postman (API Testing)
